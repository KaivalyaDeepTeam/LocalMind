# Optional: Speaker Diarization Dependencies
# Only install if you want speaker diarization feature
# Requires Python 3.10 (not compatible with Python 3.11+)

pyannote.audio>=3.1.0

# Installation:
# Option 1: Create Python 3.10 environment
#   conda create -n localmind-diarization python=3.10
#   conda activate localmind-diarization
#   pip install -r requirements.txt
#   pip install -r requirements-diarization.txt
#
# Option 2: Use without diarization (works fine!)
#   The app auto-detects if pyannote.audio is available
#   If not installed, diarization checkbox will be disabled
